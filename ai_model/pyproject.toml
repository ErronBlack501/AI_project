[project]
name = "ai-model"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Luca Heritiana TOBE", email = "123lucatobe@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "scikit-learn>=1.9.0",
]

[project.scripts]
ai-model = "ai_model:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "jupyterlab>=4.6.3",
]
